[{'rewards\', \'debt\', \'bnpl\'].includes(calculatorType)) {\n      throw new Error(\'Invalid calculator type. Must be "rewards", "debt", or "bnpl"': ''}, {'email-capture-overlay\';\n    overlay.className = \'email-capture-overlay\';\n    \n    overlay.innerHTML = `\n      <div class="email-capture-modal">\n        <button class="email-capture-close" aria-label="Close">&times;</button>\n        <h3>Get Alerts for Better Deals</h3>\n        <p>Be the first to know when 0% APR offers end or new signup bonuses drop for ${this.calculatorType === \'rewards\' ? \'credit cards': "this.calculatorType === 'debt' ? 'debt payoff strategies' : 'BNPL options'"}, {'Saved!': 'button.disabled = true;\n        \n        // Close after delay\n        setTimeout(() => {\n          this.hideModal();'}, {'email': 'email', 'calculator': 'calculatorType', 'timestamp': 'new Date().toISOString()', 'id': 'Date.now() // Simple unique ID'}, {'signup': ', e);\n    }\n  }\n  \n  // Dismiss the popup for 7 days\n  dismiss() {\n    try {\n      const dismissUntil = new Date();\n      dismissUntil.setDate(dismissUntil.getDate() + 7);\n      localStorage.setItem(this.dismissKey, dismissUntil.toISOString());\n    } catch (e) {\n      console.error(', 'flag': ", e);\n    }\n  }\n  \n  // Check if popup was dismissed recently\n  isDismissed() {\n    try {\n      const dismissUntil = localStorage.getItem(this.dismissKey);\n      if (!dismissUntil) return false;\n      \n      const dismissDate = new Date(dismissUntil);\n      const now = new Date();\n      \n      // If dismiss date is in the past, clear it and return false\n      if (dismissDate < now) {\n        localStorage.removeItem(this.dismissKey);\n        return false;\n      }\n      \n      return true;\n    } catch (e) {\n      console.error('Error checking dismiss flag:", 'emails': {"email-capture-admin';\n    adminPanel.className = 'email-capture-admin-panel';\n    \n    try {\n      // Get signups from localStorage\n      let signups = [];\n      const existing = localStorage.getItem('cs_email_signups": 'if (existing) {\n        signups = JSON.parse(existing);'}, 'ID,Email,Calculator,Timestamp': 'signups.forEach(signup => {\n        csvContent += `', 'signup.id}",': {'signup.email}",': {'signup.calculator}",': {'signup.timestamp}': 'n`;'}, 'class="admin-panel-header': 'h3>Email Signups (${signups.length'}, 'class="admin-signups-table': 'thead>\n                <tr>\n                  <th>ID</th>\n                  <th>Email</th>\n                  <th>Calculator</th>\n                  <th>Timestamp</th>\n                </tr>\n              </thead>\n              <tbody>\n                ${signups.map(signup => `\n                  <tr>\n                    <td>${signup.id'}}, {'type': 'text/csv;charset=utf-8;'}, "creditstudio-email-signups.csv');\n        link.style.visibility = 'hidden", {'panel': ', e);\n      adminPanel.innerHTML = `\n        <div class="admin-panel-header">\n          <h3>Email Signups</h3>\n          <button id="close-admin-btn" class="admin-close-btn">&times;</button>\n        </div>\n        <div class="admin-panel-content">\n          <p>Error loading signups: ${e.message}</p>\n        </div>\n      `;\n      \n      document.body.appendChild(adminPanel);\n      \n      document.getElementById(\'close-admin-btn\').addEventListener(\'click', "undefined') {\n  if (document.readyState === 'loading') {\n    document.addEventListener('DOMContentLoaded": 'EmailCapture.checkAdminMode);'}, {"undefined' && typeof module.exports !== 'undefined": {}}]